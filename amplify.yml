version: 1
frontend:
  phases:
    postBuild:
      commands:
        - node js/server.js
artifacts:
  baseDirectory: .amplify-hosting # Adjust if necessary
  files:
    - "**/*"
cache:
  paths:
    - node_modules/**/*
